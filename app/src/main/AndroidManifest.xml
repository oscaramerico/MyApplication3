<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.perusinanemiav1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroNino" />
        <activity android:name=".Principalv1" />
        <activity android:name=".TabExampleMainActivity" />
        <activity android:name=".RegistroApoderado" />
        <activity android:name=".ControlActivity" />
        <activity android:name=".AyudaActivity" />
        <activity android:name=".Dietas"></activity>

    </application>

</manifest>